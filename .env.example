DATABASE_URL=          # your Neon Postgres connection string
SITE_PASSWORD=         # shared password for the login gate
SESSION_SECRET=        # random string used to sign the session cookie, e.g. `openssl rand -base64 32`
